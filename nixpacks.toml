[phases.preinstall]
cmds = ["npm install -g next"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmds = ["npm start"]

[environment]
NODE_VERSION = "20" # Replace with the Node.js version your project requires
