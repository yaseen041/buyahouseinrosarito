# nixpacks.toml

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmds = ["npm run start"]

[environment]
NODE_VERSION = "16" # Replace with the Node.js version required for your project
